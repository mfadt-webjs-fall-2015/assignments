/*var app = app || {};
app.viz = {
	drawStuff: function(countsObj){
		var num = (Object.keys(countsObj).length);
		var numArray = new Array(num);

		setup: function(){
			var myCanvas = createCanvas(900, 900);
			myCanvas.parent('canvas');
		}
	}
	

}*/

